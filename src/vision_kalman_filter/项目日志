##项目日志##

2.22
  任务发布

2.24
    拆分任务，规划项目结构，完成文件构建（新建文件夹）
    进行项目的可行性测试（玩几局）
    注意:1.鼠标可以控制转向和发射
        2.子弹反弹后不具有碰撞体积
        3.任意一方最多同时出现三个
    难度梯度:1.中杯为单纯射击，有的种子会有几个友军遮挡情况
          2.大杯加入不同的速度，友军遮挡更加平凡
          3.超大杯加入变速机制，有悬停与反向机制
              -->注意到有的敌方处于长期遮挡情况(出现5秒只有一秒窗口期)
    策略：优先中杯，在中杯稳定后尝试考虑大杯，超大杯目测难度过高，不考虑

2.25
    测试ROS接收数据
    1.发布话题：/image_raw
    2.图像参数:
        尺寸:1152*648
        编码:rgb8(每个像素3字节:R,G,B)
        step:3456字节(一行数据长度，正好是 1152*3)


    测试串口发送
    串口：/dev/pts/0  /dev/pts/1  /dev/pts/2  /dev/pts/3
    测试方法：向串口发送几个固定方向和开火指令-->炮台正常转向，发射正常
        -->串口发送后炮台无法通过鼠标控制
    问题：串口一直在变化，导致每次需要更改重新测试
        -->解决：询问得知串口为系统自动分配，测试发现按一定顺序打开终端可保证串口一致，目前使用“/dev/pts/2“

2.26
    构建卡尔曼滤波部分
    参考:openCV的卡尔曼滤波写法与公式
    适配：噪声符合高斯分布
    要点：考虑四维向量,使用距离差计算,std::atan2(dx, dy)计算角度
    修改地方: 
        1.由于x和y方向的运动完全独立,忽略它们之间的相关性(协方差为0)，大幅降低计算量。
        2.模型固定为恒速度(适配中杯和大杯),目前不考虑加速度与反向
        3.Q矩阵不是固定的,而是Q*dt
        4.只保留方差，忽略协方差-->优化内存与计算速度，避免丢失精度
    QR的设置:
        R固定为2.0
        Q_POS = 0.5(初值)-->2.28调整 Q_POS = 0.4 -->3.2调整 Q_POS = 0.4
        Q_VEL = 0.5(初值)-->2.28调整 Q_VEL = 0.6 -->3.2调整 Q_VEL = 0.4
        同时调大至1.0以上与同时调小至0.1均影响精度
  

2.27
    openCV学习
        蹬眼法发现装甲板长68左右宽31左右
    放弃思路：检测炮台颜色后只输入相反颜色，简化计算量与流程
            问题：会在添加防友伤时需要重构项目

    构建openCV_tools类
    ai快速生成建议python测试文件,检验功能
    参数：炮台中心点坐标:(575.5f,611.5f)
    问题:openCV坐标系以左上为原点,而炮台射击系以炮台右侧为正方向-->加上角度转换，避免大的修改

2.28
    构建VisionNode类
    1.每个装甲板都设置独立追踪器-->可能导致过多的内存开销,目前目标最多三个
        -->修改:为友方添加追踪器,实测可以运行,如考虑内存限制需限制
    问题:单目标长时间追击会预测延误
        -->设置射击次数限制,达到上限切换目标或者重新射击
    

3.1
    加入置信度设置-->解决多目标选择问题与丢失问题
        1.考虑因素:年龄（追踪时间）,距离,速度,丢失,遮挡（新加入）
            开火条件:大于0.6且未遮挡
            惩罚机制:丢失严重置信度直接设为0.3
            历史存储：计算平均值，避免跳变
        2.丢失处理：设置为丢失或击毁,在新目标建立新的追踪器
        3.成果:多目标选择灵活
    成果:中杯如果运气好可以达到70分,平均50-60
        -->优化中杯，转战大杯

3.2
    调整openCVTOOLS加入可视化界面,可视化目标信息与计算数值，观察判断的准确性-->可能带来内存开支
    加入射击时预测线
    调整Q值
    成果成果:中杯平均60-70,大杯平均30(友伤扣分严重)

3.3
    加入防友伤功能
        -->只规避炮台与敌人连线之间的友方
        放弃思路：同时规避连线后方的敌人
        原因：提高瞄准的准确率可以避免子弹击中后方
        添加:友方计算需要额外半径"60.0f",为计算和规避提供时间

3.4
    优化项目注释，调试参数
    成果:大杯平均40+   -->置信度计算权重仍需优化
    总结:一个好的种子很重要(4069917219)
    

3.5
    学习双线程
    加入双线程
        优点：可以加快循环速度，减少长耗时带来的预测位置变动
        语法:std::thread,processing_thread.detach(),
            std::this_thread::sleep_for(std::chrono::milliseconds(5))
        问题:界面崩溃闪退
        解决:std::atomic<bool>

3.10
    验收
    大杯60+

3.18:下移中心点坐标，补偿距离导致判断失误

3.19:移回中心点

3.20
    完善卡尔曼
        加入预测补偿,根据运动方向和y坐标差计算补偿参数,弥补预测的滞后性
                        -->垂直距离越大 → 目标越远 → 飞行时间越长 → 误差累积越大
                        针对离散系统、卡尔曼滤波滞后和简化模型的误差修正
    问题:1.高速情况下补偿不够，需要考虑加入速度影响因子
        2.补偿后友伤判断需要修改（目前影响不大）
    成果:大杯稳定80+


4.6
    修改卡尔曼为加速度预测
    计算是未考虑高度影响，加入补偿系数
    大杯90+,超大杯30-40 --> 1.友伤严重，友伤计算为定植，应加入预测或简单计算
                            2.加速度预测有滞后性，预测不准



4.15
    创建 SerialComman类
        接收“control_command”
        加入互斥锁（代办）
        完善main


4.16
    修改main创建发布者,发布“control_command”
    创建 YAML 参数文件，从外部修改
        (yaml直接覆盖)

4.17
    加速度预测方式重做->更相信预测
    加速度为零时
        COMPENSATION_FACTOR: 0.055
        VEL_COMPENSATION_FACTOR: 0.917
    2199969087
    3689292960 //慢速种子
    串口名改为可调参数
    pub完善
    问题:ros通信未接通,pub无问题
        参数调整


4.18
    qos匹配问题修改->QoS(Quality of Service,服务质量)是ROS 2中控制通信可靠性的机制,可配置历史记录、深度、可靠性
                    （可靠/尽最大努力）、持久性等参数，确保节点间数据传输的实时性与稳定性。
                    可靠性(Reliable/Best Effort)、历史记录(Keep last/all)、深度。匹配不当会导致丢包。
    依赖同一个自定义消息文件->先创建一个专门的消息包,然后其他所有节点包都依赖它
                            单独编译,也可colcon同时编译
    用 "/dev/pts/2" 串口效果最好

4.19
    参数调整
    launch文件添加
