# 视觉卡尔曼滤波器 - RM装甲板自瞄系统处理流程（文字版）

## 一、系统初始化阶段

**步骤1:节点启动**
- ROS2节点初始化,创建`VisionNode`实例
- 初始化图像订阅器`ROSImageSubscriber`
- 尝试打开串口，记录串口状态（成功/失败）
- 初始化各类参数：击中次数(3次)、发射间隔(250ms)、友军判定半径(80像素)等
- 创建OpenCV显示窗口"装甲板跟踪"

**步骤2:处理线程启动**
- 创建独立的图像处理线程
- 设置运行标志`running_ = true`
- 线程进入循环等待图像数据

## 二、每帧图像主处理流程

**步骤3:获取图像帧**
- 检查是否有新图像,若无则等待5ms后重试
- 获取最新图像帧，若图像为空则跳过本轮处理
- 将图像设置到`cv_tools_`工具类中
- 帧计数器`frame_count_`加1

**步骤4:炮台检测与身份确认**
- 检查是否已检测到炮台(`cannon_detected_`)
- 若未检测到，调用`cv_tools_.DetectCannon()`检测炮台
- 检测成功则获取炮台颜色，确定己方颜色和敌方颜色
- 红色方：己方为红，敌方为蓝；蓝色方：己方为蓝，敌方为红

**步骤5:装甲板检测**
- 调用`cv_tools_.FindEnemyArmorCenters()`检测所有敌方装甲板中心点
- 根据己方颜色调用`cv_tools_.FindFriendlyArmorCenters()`检测友方装甲板
- 将所有检测结果合并为`all_detections`列表，每个元素包含(位置坐标, 是否敌方)

## 三、目标跟踪与信息更新

**步骤6:更新跟踪器(核心步骤)**
- 设置匹配阈值50像素
- 遍历现有所有跟踪器：
  - 获取跟踪器预测位置
  - 寻找最近的未匹配检测点
  - 若距离小于阈值则匹配成功：
    * 用检测点更新卡尔曼滤波器
    * 更新目标信息：位置、速度、敌我属性、颜色、年龄+1
    * 计算置信度并更新历史队列
    * 重置丢失帧计数器为0
    * 标记跟踪器和检测点为已匹配
  - 若匹配失败：
    * 丢失帧计数器+1
    * 标记目标为丢失状态
    * 若丢失超过5帧,降低置信度至0.3
- 处理未匹配的检测点：
  * 创建新的卡尔曼跟踪器
  * 初始化目标信息:ID、位置、敌我属性、颜色、年龄=1
  * 计算初始置信度并记录
  * 初始化丢失帧计数器为0,摧毁标记为false

**步骤7:目标分类更新**
- 清空敌方ID列表`enemy_target_ids_`和友方ID列表`friendly_target_ids_`
- 遍历所有目标信息：
  - 跳过已摧毁的目标
  - 若`is_enemy`为true,加入敌方列表
  - 否则加入友方列表

**步骤8:检查丢失目标**
- 遍历所有丢失帧计数器
- 若丢失帧数超过最大值(30帧)且未被摧毁：
  - 若是敌方目标，丢失敌方计数+1
  - 标记为已摧毁
  - 目标状态设为非活跃
  - 若此目标是当前目标,清除当前目标ID

**步骤9:遮挡检测**
- 获取所有友方目标的预测位置(提前0.2秒预测)
- 遍历所有敌方目标：
  - 计算与每个友军预测位置的距离
  - 若距离小于遮挡阈值(30像素)，标记为被遮挡
  - 检查是否在图像边缘(x<50或x>1100或y<50或y>600),若是也标记为遮挡
  - 若被遮挡,置信度降低30%

## 四、目标选择与决策

**步骤10:选择目标**
- **情况1:当前目标有效**
  - 检查当前目标ID是否有效且未被摧毁
  - 检查丢失帧数<10且目标可靠(置信度>0.6)且为敌方
  - 若满足条件，锁定计数器+1,继续使用当前目标
  
- **情况2:需要选择新目标**
  - 重置当前目标ID为-1,锁定计数器为0
  - 遍历所有敌方目标ID:
    * 过滤条件：未摧毁、丢失<20帧、可靠(置信度>0.6)
    * 计算与炮台中心的距离，过滤超出射程的目标
    * **计算优先级得分**:
      - 距离得分 = 1000 / 距离
      - 角度得分 = 10 / (角度差+1)
      - 年龄得分 = 1 + 年龄/100
      - 剩余生命得分 = 1 + (需击中次数-已击中次数)*0.3
      - 速度得分 = 1 + 速度/500
      - 置信度因子 = 当前置信度
      - 遮挡惩罚 = 被遮挡则*0.5
    * 保存每个目标的得分
  - 若有候选目标，按得分排序，选择最高分者作为新目标
  - 锁定计数器设为1

**步骤11:安全检测**
- 若无当前目标，返回"不安全-无目标"
- **检查置信度**：目标置信度>0.6?否则风险+0.3
- **检查遮挡**：目标未被遮挡？否则风险+0.4
- **检查弹道友军**:
  - 计算从炮台到目标的弹道方向向量
  - 获取所有友军预测位置(提前0.2秒)
  - 对每个友军：
    * 计算友军在弹道上的投影距离
    * 若投影距离>0且<目标距离，计算垂直距离
    * 若垂直距离<友军半径(80像素)，判定友军在弹道上
  - 若有友军在弹道上，风险+0.5,防止友伤计数+1
- **综合判断**：风险<0.6且置信度合格、无遮挡、无友军在弹道 → 安全可开火
- 生成相应的警告信息

## 五、开火控制

**步骤12:安全开火决策**
- 执行安全检测，若不通过则记录警告
- **特殊情况处理**:若目标被遮挡超过10帧,考虑切换目标
- **检查发射间隔**:距离上次开火时间是否超过250ms
- **计算目标角度**:
  - 通过卡尔曼滤波器计算目标角度
  - 角度转换：`target_angle = -target_angle + 90.0f`
  - 角度平滑：`smoothed_angle = 平滑因子×旧值 + (1-平滑因子)×新值`
- **发送转向命令**:通过串口发送当前平滑角度
- **检查角度误差**:计算平滑角度与目标角度的差值,超过5度则不开火
- **检查锁定帧数**:需要连续锁定5帧以上才开火
- **开火前最终确认**:
  - 等待20毫秒使炮台稳定
  - 再次执行快速安全检查
  - 若不通过则放弃开火
- **执行开火**:
  - 通过串口发送开火命令
  - 记录开火时间
  - 目标击中次数+1
  - **得分更新**:
    * 击中敌方:hit_enemy+1
    * 摧毁敌方(累计3次):destroy_enemy+1
    * 击中友方:hit_friend+1(理论上不会发生)
    * 摧毁友方:destroy_friend+1
  - **摧毁判定**:若击中次数达到3次
    * 标记目标为已摧毁
    * 目标状态设为非活跃
    * 清除当前目标ID
    * 重置锁定计数器

## 六、可视化与状态输出

**步骤13:绘制结果**
- 调用`cv_tools_.drawDetections()`绘制检测到的装甲板
- 绘制友军预测位置（半透明圆圈）
- 绘制所有跟踪目标：
  - **颜色编码**:
    * 绿色：当前锁定目标
    * 红色：敌方目标
    * 蓝色：友方目标
    * 橙色：被遮挡目标
    * 灰色：已摧毁目标
  - 绘制目标位置圆圈和速度向量箭头
  - 标注信息:敌我标识、ID、击中次数/需击中次数、年龄、置信度、遮挡标记
- 绘制弹道线（当前目标）
- 显示状态信息：
  * 左上角:帧数、敌我目标数量、总分、串口状态、安全状态、FPS
  * 安全警告信息
  * 当前目标详细信息:ID、锁定计数、角度、击中次数、置信度、遮挡状态

**步骤14:状态输出(每秒一次)**
- 打印当前状态：帧数、敌我数量、当前目标、锁定计数、总分、安全状态
- 若有当前目标，打印详细信息：距离、速度、置信度、遮挡状态、击中次数

**步骤15:循环等待**
- 等待1ms(OpenCV的waitKey)
- 回到步骤3,处理下一帧图像

