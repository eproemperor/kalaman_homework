## 1. Linux串口编程核心概念

### 1.1 一切皆文件

### 1.2 open()标志详解
```cpp
::open(port_name.c_str(), O_RDWR | O_NOCTTY | O_NDELAY)
```

| 标志 | 作用 | 教学意义 |
|-----|------|---------|
| **O_RDWR** | 读写模式 | 串口需要双向通信 |
| **O_NOCTTY** | 不成为控制终端 | 防止信号干扰(如Ctrl+C) |
| **O_NDELAY** | 非阻塞模式 | open()立即返回,不等待DCD信号 |

## 2. 终端I/O配置详解

### 2.1 termios结构体
```cpp
struct termios options;
tcgetattr(fd, &options);  // 获取当前配置
tcsetattr(fd, TCSANOW, &options);  // 设置新配置
```

**termios是UNIX终端I/O的核心**,包含5类标志:

| 标志类型 | 作用 | 示例 |
|---------|------|------|
| **c_cflag** | 控制模式 | 波特率、数据位、校验位 |
| **c_lflag** | 本地模式 | 回显、规范输入 |
| **c_iflag** | 输入模式 | 流控、字符转换 |
| **c_oflag** | 输出模式 | 输出处理 |
| **c_cc** | 特殊字符 | 超时、最小字符数 |



### 2.3 控制模式标志详解
```cpp
options.c_cflag |= (CLOCAL | CREAD);    // 基本设置
options.c_cflag &= ~CSIZE;               // 清除数据位
options.c_cflag |= CS8;                   // 8位数据
options.c_cflag &= ~PARENB;               // 无校验
options.c_cflag &= ~CSTOPB;               // 1位停止位
options.c_cflag &= ~CRTSCTS;              // 无硬件流控
```

**每个标志的教学意义：**

| 标志 | 作用 | 为什么这么设置 |
|-----|------|---------------|
| **CLOCAL** | 忽略调制解调器控制线 | 没有真正的调制解调器 |
| **CREAD** | 启用接收器 | 必须启用才能接收数据 |
| **CS8** | 8位数据位 | 通常传输二进制数据 |
| **~PARENB** | 无校验位 | 现代通信很少用校验 |
| **~CSTOPB** | 1位停止位 | 标准配置 |
| **~CRTSCTS** | 禁用硬件流控 | 通常用软件流控或不用 |

### 2.4 原始模式配置
```cpp
// 本地模式 - 禁用规范输入
options.c_lflag &= ~(ICANON | ECHO | ECHOE | ISIG);

// 输入模式 - 禁用字符处理
options.c_iflag &= ~(IXON | IXOFF | IXANY);
options.c_iflag &= ~(INLCR | ICRNL | IGNCR);

// 输出模式 - 禁用输出处理
options.c_oflag &= ~OPOST;
```

**为什么要用原始模式？**

| 模式 | 用途 | 适合场景 |
|-----|------|---------|
| **规范模式(canonical)** | 行缓冲，支持编辑 | 终端输入(如bash) |
| **原始模式(raw)** | 立即传输，无处理 | 二进制数据传输 |

**规范模式的问题：**
- 需要按回车才发送数据
- 会自动处理退格、删除等字符
- 会转换换行符(\n -> \r\n)

**原始模式的好处：**
- 每个字符立即传输
- 不修改数据内容
- 适合自定义协议

### 2.5 超时设置
```cpp
options.c_cc[VMIN] = 0;   // 最小读取字符数
options.c_cc[VTIME] = 10; // 超时时间（单位：0.1秒）
```

**读取行为组合：**

| VMIN | VTIME | 行为 |
|-----|-------|------|
| 0 | 0 | 非阻塞，立即返回 |
| 0 | >0 | 定时读取，超时返回 |
| >0 | 0 | 阻塞直到读取足够数据 |
| >0 | >0 | 等待数据或超时 |


## 3. 二进制协议设计

### 3.1 简单协议结构
```cpp
// 转向指令:1字节ID + 4字节float
unsigned char buffer[5];
buffer[0] = 0x01;  // 指令ID

// 开火指令:1字节ID
unsigned char buffer[1];
buffer[0] = 0x02;
```

**协议设计的教学点：**

**为什么用ID区分指令?**
```
[ID=0x01][数据...]  // 转向指令
[ID=0x02]           // 开火指令
```
- 接收端根据ID解析不同长度的数据
- 可扩展更多指令类型

### 3.2 浮点数传输
```cpp
// 将float转换为字节数组(小端序)
unsigned char* float_bytes = reinterpret_cast<unsigned char*>(&angle);
for (int i = 0; i < 4; i++) {
    buffer[1 + i] = float_bytes[i];
}
```

**浮点数序列化要点：**

1. **字节序问题**:
```cpp
// 小端序(x86):低地址存低位字节
angle = 3.14
内存地址: [0] [1] [2] [3]
数据:    0xC3 0xF5 0x48 0x40

// 大端序（网络序）：低地址存高位字节
数据:    0x40 0x48 0xF5 0xC3
```

2. **更好的序列化方法**:
```cpp
// 方法1:memcpy(安全)
uint8_t buffer[5];
buffer[0] = 0x01;
memcpy(&buffer[1], &angle, sizeof(float));

// 方法2:union(清晰)
union {
    float f;
    uint8_t bytes[4];
} converter;
converter.f = angle;
memcpy(&buffer[1], converter.bytes, 4);
```

### 3.3 同步机制
```cpp
tcdrain(fd);  // 等待数据发送完成
```

**为什么需要tcdrain?**
- write()返回可能只是数据拷贝到内核缓冲区
- tcdrain()等待硬件发送完成
- 确保时序关键的命令被真正发出

## 4. 错误处理机制

### 4.1 层次化错误处理
```cpp
bool SerialComm::open() {
    fd = ::open(...);
    if (fd == -1) {
        last_error = "无法打开串口设备: " + port_name;
        return false;
    }
    
    if (!configurePort()) {
        ::close(fd);  // 清理已打开的资源
        fd = -1;
        return false;
    }
}
```

**错误处理原则：**
1. **及早返回**：出错立即返回
2. **资源清理**：确保已分配的资源被释放
3. **错误信息**：记录有意义的错误描述

### 4.2 状态跟踪
```cpp
bool is_open;        // 串口状态
std::string last_error;  // 最后错误信息
```

**状态管理的意义：**
- 防止在未打开时操作
- 提供错误诊断信息
- 便于调试
